# Stage 3 Terminal 版本接入 - C: 终端渲染

## 约束
- 任何单个代码文件 **不得超过 200 行**，需要拆分时必须拆分。
- 只按本文件指示实现，默认你**不懂架构**、只会按步骤写代码。
- 遇到不确定规则先写入 `docs/` 提问记录，不要自作主张。
- 每完成本文件任务，必须在 `docs/implementation/summarys/` 写执行情况。

## Instructions
按顺序完成以下步骤：
1. 实现 ASCII UI 渲染函数（字符串拼接）
2. 渲染 HP/Ammo/Time/5x5 Vision
3. 标记玩家与 agent
4. 显示 last sound / memory 字段占位
5. 输出必须无多余换行

## Checklist
- [ ] 实现 ASCII UI 渲染函数（字符串拼接）
- [ ] 渲染 HP/Ammo/Time/5x5 Vision
- [ ] 标记玩家与 agent
- [ ] 显示 last sound / memory 字段占位
- [ ] 输出必须无多余换行

## 完成后必须做
- 在 `docs/implementation/summarys/` 新建 `stage3c_summary.md`，写清楚本步骤做了什么、遇到什么问题、未完成项。
- 执行 `git add .` 并 `git push`（统一提交，别只推单个文件）。

