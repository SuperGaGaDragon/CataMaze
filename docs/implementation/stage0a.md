# Stage 0 需求定稿与脚手架 - A: 阅读与范围锁定

## 约束
- 任何单个代码文件 **不得超过 200 行**，需要拆分时必须拆分。
- 只按本文件指示实现，默认你**不懂架构**、只会按步骤写代码。
- 遇到不确定规则先写入 `docs/` 提问记录，不要自作主张。
- 每完成本文件任务，必须在 `docs/implementation/summarys/` 写执行情况。

## Instructions
按顺序完成以下步骤：
1. 通读 `docs/catamaze.md` 并提炼硬性规则清单（不写代码只整理要点）
2. 列出所有待实现模块与文件清单（后端/前端/存储/agents）
3. 确认地图格式要求：50x50、S/E 语义、#/. 含义
4. 确认数据库自动迁移要求与 `DATABASE_URL` 使用方式
5. 整理 API 列表与入参/出参草案（JSON 结构草案即可）

## Checklist
- [ ] 通读 `docs/catamaze.md` 并提炼硬性规则清单（不写代码只整理要点）
- [ ] 列出所有待实现模块与文件清单（后端/前端/存储/agents）
- [ ] 确认地图格式要求：50x50、S/E 语义、#/. 含义
- [ ] 确认数据库自动迁移要求与 `DATABASE_URL` 使用方式
- [ ] 整理 API 列表与入参/出参草案（JSON 结构草案即可）

## 完成后必须做
- 在 `docs/implementation/summarys/` 新建 `stage0a_summary.md`，写清楚本步骤做了什么、遇到什么问题、未完成项。
- 执行 `git add .` 并 `git push`（统一提交，别只推单个文件）。

