# Stage 3 Terminal 版本接入 - E: 联调

## 约束
- 任何单个代码文件 **不得超过 200 行**，需要拆分时必须拆分。
- 只按本文件指示实现，默认你**不懂架构**、只会按步骤写代码。
- 遇到不确定规则先写入 `docs/` 提问记录，不要自作主张。
- 每完成本文件任务，必须在 `docs/implementation/summarys/` 写执行情况。

## Instructions
按顺序完成以下步骤：
1. 从终端完成一局最小流程（new->move->tick）
2. 检查 obs 更新是否正确
3. 检查报错提示是否清晰
4. 把所有错误打印到终端
5. 更新 `docs/terminal_usage.md`

## Checklist
- [ ] 从终端完成一局最小流程（new->move->tick）
- [ ] 检查 obs 更新是否正确
- [ ] 检查报错提示是否清晰
- [ ] 把所有错误打印到终端
- [ ] 更新 `docs/terminal_usage.md`

## 完成后必须做
- 在 `docs/implementation/summarys/` 新建 `stage3e_summary.md`，写清楚本步骤做了什么、遇到什么问题、未完成项。
- 执行 `git add .` 并 `git push`（统一提交，别只推单个文件）。

