# Stage 4 UI 版本接入 - D: 输入控制

## 约束
- 任何单个代码文件 **不得超过 200 行**，需要拆分时必须拆分。
- 只按本文件指示实现，默认你**不懂架构**、只会按步骤写代码。
- 遇到不确定规则先写入 `docs/` 提问记录，不要自作主张。
- 每完成本文件任务，必须在 `docs/implementation/summarys/` 写执行情况。

## Instructions
按顺序完成以下步骤：
1. 绑定 WASD 移动
2. 绑定 IJKL 射击
3. 绑定 ESC 清空队列
4. 支持多输入进入队列
5. 在 HUD 显示当前队列长度

## Checklist
- [ ] 绑定 WASD 移动
- [ ] 绑定 IJKL 射击
- [ ] 绑定 ESC 清空队列
- [ ] 支持多输入进入队列
- [ ] 在 HUD 显示当前队列长度

## 完成后必须做
- 在 `docs/implementation/summarys/` 新建 `stage4d_summary.md`，写清楚本步骤做了什么、遇到什么问题、未完成项。
- 执行 `git add .` 并 `git push`（统一提交，别只推单个文件）。

